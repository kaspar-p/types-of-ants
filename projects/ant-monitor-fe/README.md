# ant-monitor-fe

A wrapper for a deployment of [Grafana](https://grafana.com), the monitor
visualization system that typesofants uses. It pulls and visualizes graphs from
`ant-monitor`, the Prometheus instance.

There's a typesofants wrapper for the project because the deployment mechanism
is unique.
