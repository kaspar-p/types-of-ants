# ant-monitor

A wrapper for a deployment of [prometheus](https://prometheus.io/), the
monitoring system that typesofants uses.

There's a typesofants wrapper for the project because the deployment mechanism
is unique.
